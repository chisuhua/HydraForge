# Added by code-review-graph
.code-review-graph/

# Build artifacts
build/
# Root symlink to build/compile_commands.json (for clangd, etc.) — generated, do not commit
/compile_commands.json
# ThreadSanitizer build directory (rebuild with: cmake -B build_tsan -DAGENTICDSL_ENABLE_TSAN=ON)
build_tsan/
# ctest temporary files (cost data, last test log, etc.)
Testing/Temporary/

# Async dependencies (cloned manually; rebuild via CMake add_subdirectory)
external/async_simple/
# external/taskflow/ was previously .gitignored (2026-07-15 fix: AgentForge FetchContent
# downstream consumers need taskflow). Now vendored at commit Hash (v3.9.0 taskflow
# source retained as plain files; benchmarks/docs/doxygen omitted to keep repo small).

# Sisyphus orchestration
.omo/
.sisyphus/
# Git worktrees (per-machine ephemeral, see openspec/changes/2026-06-15-residual-engine-h-decoupling/)
.worktrees/

# Tool configurations
.claude/
.mcp.json
.opencode/
.opencode.json
# VS Code Copilot custom instructions (per-developer)
.github/code-review-graph.instruction.md
# OpenSpec: changes/ 目录入 git (团队共享 proposals/specs/tasks);
# 但 archive/ + config.yaml + specs/ 仍是 per-machine ephemeral
openspec/changes/archive/
openspec/config.yaml
openspec/specs/

# Python cache
__pycache__/
*.pyc
*.pyo
build_ninja/
.cache/
.rddf/wt/
.rddf/wt-backup/
.rddf/state/.env-cache.json
# _lib is a per-machine symlink to ~/.agents/skills/_lib/ (rdd-workflow canonical)
# Each developer creates this themselves; see AGENTS.md §环境 setup
_lib
# roadmap-state.json is per-machine ephemeral state (regenerated by guide-roadmap)
.rddf/state/roadmap-state.json
