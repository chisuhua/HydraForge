## ADR 概述

ADR-0083 定义 **IEvaluator / RewardSignal** 评估契约,Oracle 评审 (session `ses_fcba5e477ffeG9wEBHVhU64J0o`) 识别为架构层 G10 缺口——22 项已 ship 能力中**没有任何一项能回答"这次执行好不好"**,GEPA/AFlow/fine-tune/行为克隆全部依赖评估信号。本 ADR 是**至少 6 个下游 ADR (0061-09 / 0061-08 / 0061-07 / 0074 / 0078 / 0061-03) 的硬前置**,无 IEvaluator 契约则自进化方向无法启动。

**ADR 文件**: `docs/adr/adr-0083-evaluator-reward-contract.md`
**状态**: 🔍 Proposed → (待自审)
**关联 Gap**: G10 (评估/奖励信号契约缺失)
**关联 TD 项**: T15 (Trajectory IR) / T19 (GEPA) / T21 (Prompt Evidence Gate) / T22 (Fine-tune)
**Oracle session**: `ses_fcba5e477ffeG9wEBHVhU64J0o`
**关联 capability-map**: §二 G10 行 + §八 评估契约 + §八.5 优先级排序

---

## Self-Review Checklist (12 项通用)

### A. 设计完整性 (A1-A4)
- [ ] **A1. 背景与上下文**: §背景 段清晰说明 Oracle 评审关键发现 (22 项 ship 能力无评估信号) + 6 个下游 ADR 依赖关系表
- [ ] **A2. 决策 (Decision)**: §决策 列 5 项 (双层契约 / 3 内置评估器 / 与 ToolResult 关系 / 集成点 / V1 简化),每项含推荐方案与理由
- [ ] **A3. 不变量 (Invariants)**: §不变量 列 3 项 (IEvaluator 纯虚 / RewardSignal.scalar ∈ [-1.0, 1.0] / 评估器线程安全)
- [ ] **A4. 触发条件**: §触发 列出 V1 ship 条件 (含 ExecutionTrace 最小结构定义, T15 集成时同步演进)

### B. 风险与备选 (B1-B4)
- [ ] **B1. 风险评估**: §风险 列 ≥3 项 (ADR-0057 零实施重蹈 / ExecutionTrace 循环依赖 / 评估器线程安全),含影响范围与缓解
- [ ] **B2. 备选方案**: §备选 列 ≥2 项 (直接用 ToolResult.ok vs 完整 RLHF reward vs 仅 TaskSuccess 子集),解释为何不采纳
- [ ] **B3. 反向影响**: 不影响已 ship 能力 (CognitiveWorker / DomainWorkerPool 仅加可选 evaluate() 注入点)
- [ ] **B4. 安全/合规评估**: Oracle session `ses_fcba5e477ffeG9wEBHVhU64J0o` 替代安全角色评审

### C. 实施与依赖 (C1-C4)
- [ ] **C1. 实施计划**: §实施 列 5 步 (契约层定义 → TaskSuccessEvaluator 3 行实现 → CognitiveWorker 注入 → DomainWorkerPool 注入 → 单元测试)
- [ ] **C2. 依赖关系**: ADR-0061-02 (✅) / ADR-0074 (🔍) / ADR-0078 (🔍) / ADR-0061-09 (✅ Approved + V1 Shipped 2026-08-27) 全部存在且状态正确
- [ ] **C3. 契约层一致性**: 与 `include/agenticdsl/contract/` 现有 IInteractionBus 协调 (事件发射走 EventBuilder),不引入字段冲突
- [ ] **C4. 文档同步**: capability-application-map §二 G10 行 + §八.4 T19/T22 + §八.5 优先级排序,本次 ship 后同步

---

## 专用清单 (§二.1 接口契约类 ADR)

- [ ] **2.1.1 接口位置**: `evaluation/` (新目录) vs `contract/`,Oracle 评审建议 evaluation/ 以保持契约层中立
- [ ] **2.1.2 V1 范围**: 简化 (仅 TaskSuccessEvaluator 3 行实现) — 避免 ADR-0057 零实施重蹈
- [ ] **2.1.3 依赖类型**: ExecutionTrace 引用类型本 ADR §决策 定义最小结构 (trace_id, final_result, steps),T15 集成时同步演进
- [ ] **2.1.4 与现有类型关系**: 正交于 ToolResult / ErrorCode (Quality 与 ErrorCode 独立维度)

---

## 决策点 (来自 resolution-draft §一 决议 1)

| 字段 | 草案值 | 依据 |
|---|---|---|
| **会议决议** | ✅ Approved | Oracle 评审 G10 关键缺口 |
| **状态变更** | 🔍 Proposed → ✅ Approved | 通过 |
| **版本** | v1.0 | 草案即终版 |
| **下游解锁** | T15 / T19 / T21 / T22 (4 个 TD 项解除阻塞) | |
| **Gap 闭合** | G10 🔴 → ✅ Closed | Oracle 标记最高优先级 |
| **实施启动** | Sprint 24 (1 sprint) | IEvaluator 接口 + 3 行 TaskSuccess |
| **会议修订** | (待冷却期后填写) | |

**若被拒绝备选**:
- 选项 A: 推迟到 Sprint 26 (与 ADR-0023 扩展合并评审)
- 选项 B: 拆分 — 仅 `TaskSuccessEvaluator` 部分先批
- 选项 C: 合并到 ADR-0071 §D5 重写

---

## 自审决策 (24h Cooling-Off 后填写)

<!-- 创建 issue 后 24h 才能填写; 期间如发现新问题则更新此节 -->

### 最终决策

- [ ] ✅ **Approved** — 接受全部决策点, 进入实施
- [ ] ❌ **Rejected** — 拒绝, 需修改后重新评审
- [ ] ⏸ **Deferred** — 延期, 待前置条件满足

### 风险接受声明

<!-- 列出本次自审明确接受的风险, 防止"埋雷" -->

- (待冷却期后填写) 例: ADR-0083 ExecutionTrace 由本 ADR §决策 定义最小版本, T15 集成时同步演进
- (待冷却期后填写) 例: V1 仅实现 TaskSuccessEvaluator, BehavioralEquivalenceEvaluator 与 CompositeEvaluator 推迟到 V2

### 修改 (如有)

<!-- 列出自审过程中对 ADR 的修订 -->

- (待冷却期后填写)

---

## 冷却期

- **Issue 创建**: YYYY-MM-DD HH:MM
- **冷却期结束**: YYYY-MM-DD HH:MM (24h 后, 可缩短至 8h 并注明)
- **最终决策时间**: YYYY-MM-DD HH:MM
- **冷却期内变更**: (记录新增反对意见或调整)

---

## 签发

- **作者 / 评审人**: solo-dev (单人模式, 自审 = 自决)
- **日期**: YYYY-MM-DD
- **关联 Sprint**: Sprint 23 (retro approval) → Sprint 24 启动
- **关联 capability-map**: `docs/architecture/capability-application-map-2026-08.md` §二 G10 + §八.4 T19/T22
